import { Heading } from "@chakra-ui/react";
import React from "react";

const PageHeading = ({ text }) => {
  // used to set page titles
  return <Heading>{text}</Heading>;
};

export default PageHeading;
